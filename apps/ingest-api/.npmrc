# Utiliser pnpm au lieu de npm
package-manager=pnpm@10.8.0

# Remplacer le registre npm par un miroir privé (utile en entreprise)
registry=https://registry.npmjs.org/

# Activer le cache pour les dépendances
cache=~/.npm

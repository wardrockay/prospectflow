# PostgreSQL & pgAdmin Environment Variables

# PostgreSQL Configuration
POSTGRES_USER=prospectflow
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=prospectflow

# Application DB User (non-superuser; used for app + pgBouncer)
# Role name is fixed: prospectflow_app
APP_DB_PASSWORD=your_secure_app_password_here

# pgAdmin Configuration
PGADMIN_EMAIL=admin@prospectflow.local
PGADMIN_PASSWORD=your_pgadmin_password_here

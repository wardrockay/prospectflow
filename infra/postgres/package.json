{
  "scripts": {
    "db:new": "node scripts/db-new-migration.mjs",
    "db:up": "sudo docker compose up -d postgres pgadmin",
    "db:down": "sudo docker compose down -v",
    "db:logs": "sudo docker compose logs -f postgres pgadmin",
    "db:migrate": "sudo docker compose run --rm flyway",
    "db:info": "sudo docker compose run --rm flyway info",
    "db:repair": "sudo docker compose run --rm flyway repair"
  }
}
